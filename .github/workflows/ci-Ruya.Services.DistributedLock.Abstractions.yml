name: Ruya.Services.DistributedLock.Abstractions

on:
  workflow_dispatch:
    inputs:
      rollingDeployment:
        type: boolean
        description: 'Initiate rolling deployment (chain builds)'
        default: false

  repository_dispatch:
    types: []

  push:
    branches: [main, release-preview, release-qa, develop]
    paths:
      - '.github/workflows/Ruya.Services.DistributedLock.Abstractions.yml'
      - 'src/Ruya.Services.DistributedLock.Abstractions/**'

jobs:
  build:
    uses: ./.github/workflows/dotnet-library-build.yml
    with:
      project-name: 'Ruya.Services.DistributedLock.Abstractions'
      project-path: './src/Ruya.Services.DistributedLock.Abstractions/Ruya.Services.DistributedLock.Abstractions.csproj'
      # test-project-paths:
      dispatch-event-type: 'Ruya.Services.DistributedLock.Abstractions'
      triggered-branch: ${{ github.event.client_payload.branch || '' }}
    secrets:
      PAT: ${{ secrets.PAT }}
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
