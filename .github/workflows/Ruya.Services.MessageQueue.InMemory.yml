name: Ruya.Services.MessageQueue.InMemory

on:
  workflow_dispatch:
    inputs:
      rollingDeployment:
        type: boolean
        description: 'Initiate rolling deployment (chain builds)'
        default: false

  repository_dispatch:
    types: [Ruya.Services.MessageQueue]

  push:
    branches: [main, release-preview, release-qa, develop]
    paths:
      - '.github/workflows/Ruya.Services.MessageQueue.InMemory.yml'
      - 'src/Ruya.Services.MessageQueue/**'
      - 'src/Ruya.Services.MessageQueue.InMemory/**'
      - 'tests/Ruya.Services.MessageQueue.InMemory.Integration.Tests/**'

jobs:
  build:
    uses: ./.github/workflows/dotnet-library-build.yml
    with:
      project-name: 'Ruya.Services.MessageQueue.InMemory'
      project-path: './src/Ruya.Services.MessageQueue.InMemory/Ruya.Services.MessageQueue.InMemory.csproj'
      test-project-paths: './tests/Ruya.Services.MessageQueue.InMemory.Integration.Tests/Ruya.Services.MessageQueue.InMemory.Integration.Tests.csproj'
      dispatch-event-type: 'Ruya.Services.MessageQueue.InMemory'
      triggered-branch: ${{ github.event.client_payload.branch || '' }}
    secrets:
      PAT: ${{ secrets.PAT }}
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
