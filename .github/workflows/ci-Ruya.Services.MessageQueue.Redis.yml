name: Ruya.Services.MessageQueue.Redis

on:
  workflow_dispatch:
    inputs:
      rollingDeployment:
        type: boolean
        description: 'Initiate rolling deployment (chain builds)'
        default: false

  repository_dispatch:
    types: []

  push:
    branches: [main, release-preview, release-qa, develop]
    paths:
      - '.github/workflows/Ruya.Services.MessageQueue.Redis.yml'
      - 'src/Ruya.Services.MessageQueue.Redis/**'

jobs:
  build:
    uses: ./.github/workflows/dotnet-library-build.yml
    with:
      project-name: 'Ruya.Services.MessageQueue.Redis'
      project-path: './src/Ruya.Services.MessageQueue.Redis/Ruya.Services.MessageQueue.Redis.csproj'
      # test-project-paths:
      dispatch-event-type: 'Ruya.Services.MessageQueue.Redis'
      triggered-branch: ${{ github.event.client_payload.branch || '' }}
    secrets:
      PAT: ${{ secrets.PAT }}
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
