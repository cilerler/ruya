name: Ruya.Services.TokenBroker.Abstractions

on:
  workflow_dispatch:
    inputs:
      rollingDeployment:
        type: boolean
        description: 'Initiate rolling deployment (chain builds)'
        default: false

  repository_dispatch:
    types: []

  push:
    branches: [main, release-preview, release-qa, develop]
    paths:
      - '.github/workflows/Ruya.Services.TokenBroker.Abstractions.yml'
      - 'src/Ruya.Services.TokenBroker.Abstractions/**'

jobs:
  build:
    uses: ./.github/workflows/dotnet-library-build.yml
    with:
      project-name: 'Ruya.Services.TokenBroker.Abstractions'
      project-path: './src/Ruya.Services.TokenBroker.Abstractions/Ruya.Services.TokenBroker.Abstractions.csproj'
      # test-project-paths:
      dispatch-event-type: 'Ruya.Services.TokenBroker.Abstractions'
      triggered-branch: ${{ github.event.client_payload.branch || '' }}
    secrets:
      PAT: ${{ secrets.PAT }}
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
